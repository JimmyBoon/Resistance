using RPG.Core;
using UnityEngine;

namespace RPG.Control
{
    public struct NoiseInstance
    {
        public NoiseType noiseType;
        public GameObject noiseSource;
        public Vector3 noiseLocation;
        public float noiseAmount;
    }
}

